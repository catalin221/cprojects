#include <unistd.h>
#include <stdio.h>

int main() {
    char* currentWorkingDirectory = getcwd(NULL, 0);
    char* moviesFileDir;
    char* buyersFileDir;
    strcat()
    printf("Directory for movies: %s\n", moviesFileDir);
    return 0;
}
